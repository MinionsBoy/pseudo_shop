"""Test package for pseudo_shop.

This file exists to mark the ``tests`` directory as a Python package.  Your
pytest tests should live in this directory.  When you run ``pytest`` from
the project root it will automatically discover test modules whose names
begin with ``test_``.
"""